---
trigger: always_on
---

Always check if the component can be reused.

Avoid duplicate UI blocks.

Extract common UI into shared components.

If code repeats more than twice → refactor.
One component = One responsibility.

If a component grows beyond ~150 lines → split it.

Separate UI, logic, and API calls.